[package]
name = "notify"
version = "1.0.0"  # master side
authors.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
# Internal
rpguru_sdk  = { path = "../../lib/rpguru_sdk" }
db_types = { path = "../../lib/db_types" }
auth = { path = "../auth" }
base = { path = "../base" }
# The health facts this module notifies on, and the canvas/account ids they name.
orchestration = { path = "../orchestration" }

# Framework
wakuwaku = { workspace = true }
kanau = { workspace = true }
tokio = { workspace = true }

# Persistence
sqlx = { workspace = true }

# Messaging
amqprs = { workspace = true }

# gRPC / transport
tonic = { workspace = true }

# Serialization
rkyv = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

# Delivery channels
lettre = { workspace = true }
reqwest = { workspace = true }

# Data types
time = { workspace = true }

# Tracing
tracing = { workspace = true }

# Error handling
thiserror = { workspace = true }
anyhow = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }

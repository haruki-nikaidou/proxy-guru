{
	"name": "guru-graph",
	"private": true,
	"version": "0.0.1-alpha",
	"type": "module",
	"description": "The pod graph as the dashboard draws and edits it: pure TypeScript, no framework.",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"test": "bun test",
		"check": "tsc --noEmit"
	},
	"devDependencies": {
		"@types/bun": "^1.3.0",
		"typescript": "^6.0.3"
	}
}

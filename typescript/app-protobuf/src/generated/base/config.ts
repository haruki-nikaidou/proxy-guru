// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.19.1
// source: base/config.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "guru.base";

/**
 * One `app_config` row as the dashboard sees it.
 *
 * Every module that exposes its configuration does so with a typed
 * `Get<Module>Config` / `Set<Module>Config` pair on its own service, and both
 * answer with this message.
 *
 * Reads are deliberately **undecoded**: a row that no longer matches its type is
 * exactly what an operator must see, and `Set` is the way back out. Decoding on
 * read would either hide the corruption behind `Default` or make the document
 * unreadable at the moment it most needs repairing.
 */
export interface ConfigDocument {
  /**
   * Whether the key has a row at all. An unseeded installation is a normal
   * state, not an error.
   */
  stored: boolean;
  /**
   * The document as stored (pretty-printed), or `defaults_json` when `stored`
   * is false.
   */
  json: string;
  /**
   * What `manage-tool config seed` would write, always filled in, so the UI can
   * offer "reset to defaults".
   */
  defaultsJson: string;
}

function createBaseConfigDocument(): ConfigDocument {
  return { stored: false, json: "", defaultsJson: "" };
}

export const ConfigDocument: MessageFns<ConfigDocument> = {
  encode(message: ConfigDocument, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stored !== false) {
      writer.uint32(8).bool(message.stored);
    }
    if (message.json !== "") {
      writer.uint32(18).string(message.json);
    }
    if (message.defaultsJson !== "") {
      writer.uint32(26).string(message.defaultsJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigDocument {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfigDocument();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.stored = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.json = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.defaultsJson = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfigDocument {
    return {
      stored: isSet(object.stored) ? globalThis.Boolean(object.stored) : false,
      json: isSet(object.json) ? globalThis.String(object.json) : "",
      defaultsJson: isSet(object.defaultsJson)
        ? globalThis.String(object.defaultsJson)
        : isSet(object.defaults_json)
        ? globalThis.String(object.defaults_json)
        : "",
    };
  },

  toJSON(message: ConfigDocument): unknown {
    const obj: any = {};
    if (message.stored !== false) {
      obj.stored = message.stored;
    }
    if (message.json !== "") {
      obj.json = message.json;
    }
    if (message.defaultsJson !== "") {
      obj.defaultsJson = message.defaultsJson;
    }
    return obj;
  },

  create(base?: DeepPartial<ConfigDocument>): ConfigDocument {
    return ConfigDocument.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ConfigDocument>): ConfigDocument {
    const message = createBaseConfigDocument();
    message.stored = object.stored ?? false;
    message.json = object.json ?? "";
    message.defaultsJson = object.defaultsJson ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
